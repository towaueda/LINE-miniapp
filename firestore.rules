rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // 全コレクション: クライアントSDKからの直接アクセスは不要
    // 読み書き全てサーバー側 (Admin SDK) 経由のみ許可
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
